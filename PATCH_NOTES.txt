NATIVE CURVE v21.1 -- FIX: keep certified spin points
=======================================================
You were right. v21 wrongly dropped the spin chain's native points at dim
64 and 128. They are NOT garbage -- their self-check deviations are 7.4e-9
and 8.0e-9, far under the 1e-4 tolerance -- excellent, fully certified.

The cause: those spin points came from an older run that recorded the
numeric deviation but not the explicit "passed": true field. v21's filter
required that field, so it read None (missing) as "not certified" and
dropped good data. The oscillator you regenerated yesterday has the field;
the spin dim-128 run (days earlier) predates it.

FIX: the filter now falls back to comparing max_abs_dev against the
tolerance when "passed" is absent. Result:
  spin_chain      native curve -> dims 4,8,16,32,64,128   (RESTORED)
  oscillator_bath native curve -> dims 8,16,32            (dim 64 still
                                   correctly dropped: its dev is 1.9e+92)

One plot file. No re-run.
  cd C:\Users\sayak\Downloads\repo-split\qutip-bundling
  git switch result2-split
  Expand-Archive $env:USERPROFILE\Downloads\native-certified-v21-1.zip -DestinationPath . -Force
  cd benchmarks
  $env:PYTHONPATH = "..\src"
  python plot_cost_scaling.py
  python export_csv.py
  git add -A
  git commit -m "R2: native curve plotted at certified dims; fix dropped certified spin points (dev-fallback)"
  git push
