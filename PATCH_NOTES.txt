NATIVE CURVE: CERTIFIED POINTS ONLY (v21)
===========================================
One file: plot_cost_scaling.py.

You were right: showing a wall-clock time for a solve whose ANSWER cannot be
trusted is misleading. The native full-dissipator curve now plots ONLY at
dimensions where its accuracy is certified -- either mesolve also ran there
(the two exact routes matched), or the substep-halving self-check passed.
Where the self-check failed (oscillator dim 64), the point is WITHHELD, not
shown with a caveat.

Effect on your figures:
 - Oscillator: native curve now stops at dim 32 (dim 64's dark-red diamond
   disappears -- that was the uncertifiable 290 s solve).
 - Spin chain: unchanged (every native point there is certified; the curve
   still reaches dim 128).
 - mesolve, SLB, iso, Davies curves: all unchanged.

No re-run needed -- pure plotting change. But re-run export_csv too so the
CSVs match.

  cd C:\Users\sayak\Downloads\repo-split\qutip-bundling
  git switch result2-split          (this is benchmark work, not the src branch)
  Expand-Archive $env:USERPROFILE\Downloads\native-certified-v21.zip -DestinationPath . -Force
  cd benchmarks
  $env:PYTHONPATH = "..\src"
  python plot_cost_scaling.py
  python export_csv.py
  git add -A
  git commit -m "R2: plot native exact curve only at self-check-certified dimensions"
  git push

(This commit should include the fresh oscillator data you just regenerated
under v19 enforcement, if you haven't committed that yet -- git add -A picks
it up.)
