EXPORTER FIX (v14.1) - one file
Fixes the KeyError on diverged sweep rows; summary CSVs also gain the
mse / sem_sq / diverged columns. Your run and plots succeeded fully -
only re-run export_csv.py, then commit everything.

  Expand-Archive $env:USERPROFILE\Downloads\export-fix-v14-1.zip -DestinationPath . -Force   (from repo ROOT)
  cd benchmarks
  python export_csv.py
  git add -A ; git commit -m "R2: guarded oscillator data; exporter handles diverged rows" ; git push
