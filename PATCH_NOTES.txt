RESULT 2 FIGURE REVISION v11.1 (replaces v11 - delete v11 from Downloads)
===========================================================================
Same as v11 plus: x-axis ticks now show BOTH sizes at every measured point -
the Hilbert dimension and its Lindblad operator count (e.g. "32 / N_L=218").

Steps (data unchanged - re-plot only):
  cd C:\Users\sayak\Downloads\repo-split\qutip-bundling
  Expand-Archive $env:USERPROFILE\Downloads\r2-figure-revision-v11-1.zip -DestinationPath . -Force
  cd benchmarks
  $env:PYTHONPATH = "..\src"
  python plot_cost_scaling.py
  git add -A
  git commit -m "R2 figure: single panel, extrapolation guide, dual N/N_L axis; prose updated"
  git push
