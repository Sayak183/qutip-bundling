SELF-CHECK ENFORCEMENT + M* LADDER (v19)
==========================================
Two files. Your dim-128 run was a success -- keep its data. This patch
fixes what that run EXPOSED:

1. run_cost_scaling.py: a failing reference self-check now REJECTS the
   reference. At oscillator dim 64 your log shows "self-check: max dev
   1.85e+92" -- the half-substep reference diverged, proving the step size
   is marginal there, so the 8-substep reference cannot be certified. It
   was kept and used anyway (my omission). Now: rejected, that dimension
   gets no accuracy data, run continues. (Nothing downstream was corrupted:
   no M reached the target at oscillator dim 64 anyway.)

2. BENCHMARKS.md: your dim-128 result corrected the story. M* does NOT
   saturate -- the measured ladder is 1->8->16->32->32->64, i.e. roughly
   M* ~ sqrt(N). Still sublinear, still the reason the iso slope stays
   ~N^2.6 instead of N^4. The doc now states the measured ladder and the
   reference-rejection rule.

ALSO: your plot_cost_scaling.py is still on ESTIMATE_TYPE="single" from the
test pass, so your DEFAULT (ensemble) R2 figures are stale. Flip it back to
"ensemble" and re-plot before committing.

Steps (no re-run of the big job needed):
  cd C:\Users\sayak\Downloads\repo-split\qutip-bundling
  Expand-Archive $env:USERPROFILE\Downloads\selfcheck-enforce-v19.zip -DestinationPath . -Force
  cd benchmarks
  # set ESTIMATE_TYPE = "ensemble" in plot_cost_scaling.py
  $env:PYTHONPATH = "..\src"
  python plot_cost_scaling.py
  python export_csv.py
  git add -A
  git commit -m "R2: dim-128 spin data; reference self-check now rejects uncertifiable refs; M* ladder documented"
  git push

OPTIONAL, when convenient (~25 min): re-run ONLY the oscillator so its data
file carries the rejection rather than the uncertifiable reference:
  python run_cost_scaling.py --system oscillator_bath
